{
  "name": "TestServer",
  "private": true,
  "version": "0.0.0",
  "description": "A test server for app development",
  "devDependencies": {
    "http-server": "^0.6.1"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a 101.200.185.40 -p 8000 -c-1"
  }
}
